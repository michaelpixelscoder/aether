[alias]
lobby = "run -p lobby"
sandbox = "run -p sandbox"
shipwright = "run -p shipwright"
runner = "run -p runner"

[target.wasm32-unknown-unknown]
rustflags = ["--cfg", "getrandom_backend=\"wasm_js\""]
